<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA—Compatible" content="IE=edge">
        <meta name="viewport" content="width=device—width, initial-scale=l.0">
        <link href="/assets/style.css" type="text/css" rel="stylesheet" />
        <title>DOCUMENT</title>
    </head>
    <body>
        <h1>Person: <%= ID %> </h1>
        <h2>Message: <%= Message %></h2>

        /* se hace un switch que nos permita mantener algunos mensajes por defecto
        y en base al tipo de mensaje especificar cuantas veces queremos que se repita
        toda la parte de las repeticiones lo hacemos con un poderosisimo for  */
        <% switch (Message) {
            case "Hola": %>
              <% var result = repeat(Message, Time); %>
              <h2><%= result %></h2>
              <% break;
            case "Bienvenido": %> 
              <% var result = repeat(Message, Time); %>
              <h2><%= result %></h2>
              <% break;
            case "Adios": %>
              <% var result = repeat(Message, Time); %>
              <h2><%= result %></h2>
              <% break;
            default: %>
              <h2>Invalido</h2>
              <% break;
          } %>
          
          <% function repeat(message, times) {
            var result = '';
            for (var i = 0; i < times; i++) {
                result += message + ' ' + ID + ' ';
            }
            return result.trim();
          } %>
          
    </body>
</html>